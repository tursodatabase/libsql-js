{
  "name": "libsql-experimental-integration-tests",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "PROVIDER=sqlite ava && PROVIDER=libsql ava"
  },
  "devDependencies": {
    "ava": "^5.3.0"
  },
  "dependencies": {
    "better-sqlite3": "^8.4.0",
    "libsql-experimental": "^0.0.2"
  }
}
